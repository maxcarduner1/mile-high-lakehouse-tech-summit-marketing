/**
 * Brightwave Campaign Desk routes (stub).
 *
 * TODO: Adapt from Brightwave domain (campaigns, campaign actions, creatives).
 * For now, this is a placeholder to prevent import errors.
 */

import type { Application } from 'express';
import type { AppDb } from '../db/index.js';

export function registerCampaignRoutes(_app: Application, _deps: { db: AppDb }): void {
  // Placeholder — implement per Brightwave domain.
}
